[Unit]
Description=Opale maintenance backup timer for %i

[Timer]
# Backup quotidien à 03:20 UTC, avec dispersion pour éviter les pics.
OnCalendar=*-*-* 03:20:00 UTC
RandomizedDelaySec=1200
Persistent=true

[Install]
WantedBy=timers.target
