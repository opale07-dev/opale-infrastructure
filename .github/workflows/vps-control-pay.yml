name: "🔧 Opale Pay VPS Control (Start/Stop)"

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action à exécuter sur la VM'
        required: true
        default: 'stop'
        type: choice
        options:
          - start
          - stop
      server_id:
        description: 'ID OpenStack exact de la VM (recommandé si plusieurs serveurs portent le même nom)'
        required: false
        default: ''
        type: string

jobs:
  control:
    runs-on: ubuntu-latest
    steps:
      - name: Install OpenStack Client on Runner
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq python3-openstackclient > /dev/null

      - name: Control VM Instance
        env:
          OS_AUTH_URL: ${{ secrets.OS_AUTH_URL }}
          OS_PROJECT_ID: ${{ secrets.OS_PROJECT_ID }}
          OS_PROJECT_NAME: ${{ secrets.OS_PROJECT_NAME }}
          OS_USER_DOMAIN_NAME: ${{ secrets.OS_USER_DOMAIN_NAME }}
          OS_PROJECT_DOMAIN_NAME: ${{ secrets.OS_PROJECT_DOMAIN_NAME }}
          OS_USERNAME: ${{ secrets.OS_USERNAME }}
          OS_PASSWORD: ${{ secrets.OS_PASSWORD }}
          OS_REGION_NAME: ${{ secrets.OS_REGION_NAME }}
          OS_IDENTITY_API_VERSION: "3"
        run: |
          TARGET="${{ github.event.inputs.server_id }}"
          if [ -z "$TARGET" ]; then
            TARGET="opale-pay-prod"
          fi

          echo "Sending ${{ github.event.inputs.action }} signal to ${TARGET}..."
          openstack server ${{ github.event.inputs.action }} "$TARGET"

          echo "Waiting for status verification..."
          sleep 5
          STATUS=$(openstack server show "$TARGET" -c status -f value)
          echo "Current VM Status: $STATUS"
